## 1.1.13 - 2026-09-12

- Anatomy per-field validators tightened (v1.1.13). The `mode` field on `real-account-minimal-stack-up` secret rows must equal the exact string `"400"` - the value `docker exec stat -c %a` prints for owner-read-only. The v1.1.12 alternative spelling `"0400"` is now rejected; every other octal ("600", "640", "644", "755", "000", "040", "444") and every non-octal input ("", "abc", "9", "400 ", "4000", " 400") continues to FAIL.
- Reload-burst window rule extended (v1.1.13). The counting `real-account-reload-burst / AC-composeHost-zeroDowntimeReload` row now REQUIRES four positive finite-integer millisecond timestamps under `evidence.burst`: `burstStartedAt`, `burstEndedAt`, `reloadStartedAt`, `reloadEndedAt`. The cross-field rule asserts `burstEndedAt > burstStartedAt`, `reloadEndedAt > reloadStartedAt`, and the reload interval sitting wholly inside the burst interval (`burstStartedAt <= reloadStartedAt AND reloadEndedAt <= burstEndedAt`). Missing endpoints, `NaN`, `Infinity`, `-Infinity`, zero, negatives, non-integer floats, string values, degenerate intervals and reload windows straddling the burst boundary all FAIL. The counter cross-field from v1.1.12 (`total > 0`, `drops === 0`, `twoXx === total`, `overlapCount > 0`) and `clockDomain === "server"` remain in force.
- Walker `containerId` correlation is now identifier-only (v1.1.13). The 64-hex `containerId` on a counting row counts only when the record's `observedSecretModes.observations` tree carries an entry keyed by the same id - that entry is populated by `docker inspect --format {{.Id}}` and `docker exec stat`, engine evidence keyed by the id. A `containerName` present in the compose services listing or in `observedNames` is NOT identifier evidence; a row whose only correlation is a name match FAILS the walker. A synthetic negative case in the anatomy test proves it in-memory without touching `.rcf/reports/`.
- Anatomy record walk rejects empty result sets (v1.1.13). A present record whose `results` is absent, not an array, or an empty array FAILS - a record that carries a probeName but no rows attests to nothing. The v1.1.12 malformed-row rejection (evidence / limitation / reason / `notObservableHere.ac` markers) stays in force. `no local records` remains the only permitted early-return, and only when the directory itself has no record files.
- Anatomy walkers accept an optional records-directory override (v1.1.13): with `RCF_LITE_RECORDS_DIR` set the walker reads `<dir>/platform-docker-compose-host/*.json` instead of the tree default. CI never sets this variable; the operator-side evidence walk uses it. Anatomy synthetic fixtures for reload-burst keep the v1.1.12 counters (`total: 4242`, `twoXx: 4242`, `drops: 0`, `overlapCount: 42`, `reloadDurationMs: 424`) and gain synthetic window timestamps (`burstStartedAt: 1000`, `burstEndedAt: 11000`, `reloadStartedAt: 2000`, `reloadEndedAt: 3000`); live server-clock epochs never appear in branch source.
- Family records regenerated (v1.1.13). Every shipped probe on this blueprint (`caddyfile-validate`, `compose-config-lint`, `secrets-as-files-scan`, `real-account-minimal-stack-up`, `real-account-reload-burst`) was run through its run-shim with NO account variables set: offline probes wrote `conformanceOnly` rows anchored to their shipped ACs, and each real-account probe wrote its single-variable `accountBoundSkipped` row naming `CI_HAS_HETZNER_ACCOUNT`. Five record files live under `.rcf/reports/blueprints/platform-docker-compose-host/`; every row is one the current probe wrote at the current version and the strict walker validates all of them.
- Colour on E is unchanged: AMBER on every AC whose only observation is offline conformanceOnly (`AC-composeHost-healthcheckLint`, `AC-composeHost-secretsAreFiles`, `AC-composeHost-restartClassification`, `AC-composeHost-logDriverClassification`, the reverse-proxy-artefact and read-only-bind-mount clauses of `AC-38107-4` / `AC-composeHost-reverseProxyArtefactValid`); GREEN on `AC-composeHost-upClean`, `AC-composeHost-zeroDowntimeReload` and `AC-composeHost-secretShape`.

## 1.1.11 - 2026-09-11

- Anatomy applies row-specific value validators (v1.1.11) instead of null/undefined presence checks alone. `real-account-minimal-stack-up`'s upClean row requires a non-empty `observedSecretModes` whose entries each carry a 3- or 4-digit octal `mode` (400 / 0400), and at least one of a non-empty-string `observedNames` or `healthcheckKeys` array; the secretShape row requires an octal `mode`. `real-account-reload-burst` requires non-negative integer counters `total`, `twoXx`, `drops` and `overlapCount` with `twoXx <= total` and `drops === total - twoXx`, and `clockDomain === "server"`. Empty strings, empty arrays, `false`, `NaN`, negative counters and inconsistent totals now FAIL; one negative-proof test per validator is added to `packages/rcf-lite/test/blueprint/platform-docker-compose-host-anatomy.test.js`.
- Unmapped offline probes (`caddyfile-validate`, `compose-config-lint`, `secrets-as-files-scan`) MUST NOT emit `accountBoundSkipped` rows: they have no declared gate variable to skip on, so only `conformanceOnly` rows are permitted. `assertRowsCarry7dShape` now refuses a skip row from any unmapped probe; the change is proved by a paired negative / positive test.
- Anatomy synthetic fixtures carry unmistakably synthetic vendor-id integers (`424245`, `424246`) and a 64-character synthetic hex `containerId` (`deadbeef` repeated eight times). No live-account identifiers appear in branch source.
- Anatomy walks any records under `.rcf/reports/blueprints/platform-docker-compose-host/`. Every walkable counting row is validated against the per-probe map, and its mapped identifier must appear in the record's own inventory or event trail (`serverId` in `eventTrail` / `postRunInventory` / `postTeardownServerIds` / `teardown`; `containerId` in the record's `observedSecretModes` tree or via a `containerName` present in the compose `services` / `observedNames` listing); when the directory is empty or absent the walk reports `no local records` and passes.
- Colour on E is unchanged: AMBER on every AC whose only observation is offline conformanceOnly (`AC-composeHost-healthcheckLint`, `AC-composeHost-secretsAreFiles`, `AC-composeHost-restartClassification`, `AC-composeHost-logDriverClassification`, the reverse-proxy-artefact and read-only-bind-mount clauses of `AC-38107-4` / `AC-composeHost-reverseProxyArtefactValid`); GREEN on `AC-composeHost-upClean`, `AC-composeHost-zeroDowntimeReload` and `AC-composeHost-secretShape`.

## 1.1.10 - 2026-09-11

- Anatomy assertions are now driven by a per-probe requirement map (`PROBE_REQUIREMENTS` in `packages/rcf-lite/test/blueprint/platform-docker-compose-host-anatomy.test.js`). For each counting probe on this blueprint, the map declares the row's required vendor-minted identifier (with a shape check: `serverId` is a vendor integer or non-empty numeric string; the Docker `containerId` is a 64-character hex string) plus every required row-specific observation. `real-account-minimal-stack-up`'s upClean row requires the 64-hex `containerId` (nested under `evidence.observedSecretModes.observations[].containerId` on the live record), `observedSecretModes`, and one of `observedNames` or `healthcheckKeys`; the secretShape row requires the top-level 64-hex `containerId` and the observed `mode`. `real-account-reload-burst` requires the full tuple `total`, `twoXx`, `drops` and `overlapCount` (all nested under `evidence.burst` on the live record) plus `clockDomain === "server"`.
- Observation lookup traverses the whole evidence tree, so a required field nested under `evidence.burst` or `evidence.observedSecretModes.observations` still counts. Any counting row that misses one of the required fields FAILS; a counting row from an offline probe not declared in the map (`caddyfile-validate`, `compose-config-lint`, `secrets-as-files-scan`) FAILS on the spot - those probes may emit only skip / conformanceOnly / notObservableHere rows. Nine negative-proof tests in the same file demonstrate the failure modes (missing observation, cross-probe combination, non-hex64 `containerId`, `clockDomain` other than `"server"`, unmapped probe with a counting row, and nested-field deep search).
- Colour on E is unchanged: AMBER on every AC whose only observation is offline conformanceOnly (`AC-composeHost-healthcheckLint`, `AC-composeHost-secretsAreFiles`, `AC-composeHost-restartClassification`, `AC-composeHost-logDriverClassification`, the reverse-proxy-artefact and read-only-bind-mount clauses of `AC-38107-4` / `AC-composeHost-reverseProxyArtefactValid`); GREEN on `AC-composeHost-upClean`, `AC-composeHost-zeroDowntimeReload` and `AC-composeHost-secretShape`.

## 1.1.9 - 2026-09-11

- Ruling for this family, final: a counting row is a LIVE row whose identifier is a value the vendor API minted for the row (server id, snapshot id, firewall id, image id, vendor request id, or the 64-hex Docker container id from `docker inspect`) AND whose derived observation is row-specific (per the per-probe observation fields named on the anatomy test). Mock and offline rows carry no vendor-minted identifier by nature and are always `conformanceOnly` de-claims. There is no supplied/echo path for this family.
- Anatomy identifier set is narrowed to `serverId`, `snapshotId`, `firewallId`, `imageId`, `containerId`, `vendorRequestId`, `requestId`. Generic `id` and `resourceId`, and any `supplied<Name>` / `echoed<Name>` pair, are dropped. The derived-observation set is narrowed to the row-specific fields each counting probe carries (minimal-stack-up: `observedNames` / `healthcheckKeys` for healthy services and `observedSecretModes` / `mode` for the in-container mode; reload-burst: `total`, `twoXx`, `drops`, `overlapCount`, `clockDomain`). Generic `observed`, `port`, `protocol`, `direction` fields are dropped.
- Colour on E: AMBER on every AC whose only observation is offline conformanceOnly (`AC-composeHost-healthcheckLint`, `AC-composeHost-secretsAreFiles`, `AC-composeHost-restartClassification`, `AC-composeHost-logDriverClassification`, the reverse-proxy-artefact and read-only-bind-mount clauses of `AC-38107-4` / `AC-composeHost-reverseProxyArtefactValid`); GREEN on the live-observed clauses (`AC-composeHost-upClean` with the applied stack, `AC-composeHost-zeroDowntimeReload` with the on-server burst carrying `clockDomain="server"`, `AC-composeHost-secretShape` with the in-container mode observation on the 64-hex containerId). AC-composeHost-healthcheckLint is AMBER because its only observing row is offline `compose-config-lint`; the applied-stack `docker compose ps` observation lands on `AC-composeHost-upClean`, not `healthcheckLint`.

## 1.1.8 - 2026-09-11

- Anatomy identifier rule tightened: an identifier is a value the engine or vendor minted (server id, snapshot id, firewall id, image id, engine request id, or the 64-hex Docker container id from `docker inspect`). A sha256 hash the probe itself computes over an artefact it read is NO LONGER accepted as an identifier - it is a derived-by-the-probe checksum, not an engine echo - and `contentSha256` is removed from the identifier set. A container NAME Compose chose (for example `rcf-lite-throwaway-web`) is NO LONGER accepted as `containerId`; the 64-hex id from `docker inspect --format '{{.Id}}'` is what satisfies the identifier rule. Context/input fields (`expected`, `engineNote`, `event`, `service`, `path`, `source`, event names, manifest names, container names) no longer satisfy the derived-observation half either.
- Offline probes (`caddyfile-validate`, `compose-config-lint`, `secrets-as-files-scan`) carry no engine-minted identifier by nature: every result row is a `conformanceOnly` de-claim with `anchorAcId: null` and a `limitation` string beginning with a shipped AC id and naming the live probe (or the missing observation) the offline check does not observe. The hash of the observed artefact and the validator output stay on the row as derived context. The stale header comment on `secrets-as-files-scan` referring to `notObservableHere.ac` is replaced with the current `conformanceOnly` explanation.
- `real-account-minimal-stack-up` secret-mode row records the 64-hex `containerId` from `docker inspect --format '{{.Id}}'` (retrieved by the compose-stack driver after the stack is up); the Compose service name is kept as `containerName` derived context.

## 1.1.7 - 2026-09-11

- Anatomy assertions moved from key-name allow-lists towards a semantic rule: every result row's evidence must carry an engine-minted identifier (a vendor / resource id, engine request id, or a 64-hex Docker container id from `docker inspect`) AND a derived observation (excerpt, statusCode, mode, wallClockTime, non-zero count, or structured engine-returned object). Event names, file paths, service names, manifest names, resource names, and probe inputs are derived context, not identity. Note: at 1.1.7 the anatomy still accepted a probe-computed `contentSha256` as identity on offline validators and stored a Compose-chosen container NAME under `containerId` on the live secret-mode row; 1.1.8 removes both, and 1.1.9 further removes the supplied/echo pair path.
- `caddyfile-validate`, `compose-config-lint` and `secrets-as-files-scan` were extended to carry a deterministic sha256 of the observed artefact on every row as derived context. Note: 1.1.8 reclassifies these rows as `conformanceOnly` (see above); the hash remains on the row as context.
- The `real-account-reload-burst` record now carries `clockDomain: "server"` in the burst evidence body: the probe copies the fixture's `clockDomain` return field verbatim, and the regenerated live record includes it.

## 1.1.6 - 2026-09-11

- `real-account-reload-burst` overlap proof runs in ONE clock domain: the on-server undici burst script itself spawns `sudo docker compose exec -T caddy caddy reload --config /etc/caddy/Caddyfile` after a small warm-up and stamps every observation (burst start, each request `startedAt`/`endedAt`, reload start, reload end) from the server's own `Date.now()`. The runner receives one JSON blob and does NO rebase from the ssh call time. Overlap and burst-window containment are computed from server-clock stamps directly. The probe copies the fixture's `clockDomain: 'server'` return field into the burst evidence object; the regenerated live record on this branch carries that field.
- Compose YAML parser in `probe-utils.mjs` now preserves fields on a list-item object after its first `key: value` line. The long-form service-level secret entry (`- source: web-token`, `target: web-token`, `mode: 0400`) parses as one `{source, target, mode}` object, so `secrets-as-files-scan` records `declaredMode="0400"` for the `web-token` reference (previously read `unset`).
- Compose-stack driver default `--wait-timeout` for `docker compose up -d --wait` is the shipped elicited `healthcheck-timeout-seconds` value (30). `COMPOSE_UP_TIMEOUT_SECONDS` is declared in the fixture README env-var section as the optional override for the account-bound branch.
- `real-account-minimal-stack-up` observes the in-container mode of every mounted secret with `sudo docker exec <container> stat -c %a /run/secrets/<name>` and records one row per secret anchored to AC-composeHost-secretShape (a mode other than `400` FAILS the row); it also FAILS if a top-level secret has no consuming service.
- `secrets-as-files-scan` no longer assumes an unset service-level `mode` means `0o400`. The mode row is `anchorAcId: null`, `conformanceOnly: true`, with a `limitation` string that starts with `AC-composeHost-secretShape` and names the process-level observation on the real-account probe; the row does NOT use `notObservableHere` (the AC is process-observable via `docker exec stat`, not shelf-only). The offline scan continues to FAIL on undeclared secrets, on environment references bypassing service-level secrets, on orphan top-level secrets, and on plaintext-literal hits across compose.yaml, .env and every service's bind-mount source.
- Vendor references named at the module level: Docker Engine convenience install script (`https://docs.docker.com/engine/install/ubuntu/` verifiedOn 2026-09-11), Ubuntu 24.04 `nodejs` apt package (`https://packages.ubuntu.com/noble/nodejs` verifiedOn 2026-09-11), Docker Compose secret file mode default (`https://docs.docker.com/reference/compose-file/secrets/` verifiedOn 2026-09-11).
- Anchor detail prefixes: every `AC-composeHost-*` prefix carries the first eight words of the AC or REQ text it anchors.
- Anatomy assertions are strict inline: identity fields come from a fixed set (vendor / resource ids, event names, artefact paths, resource names) and observation fields come from a fixed set (excerpts, mode, statusCode, wallClockTime, vendor-return values, non-zero counts, structured derived observations). `notObservableHere` is reserved for browser-only ACs and this blueprint has none, so any `notObservableHere` row FAILS. `conformanceOnly` rows must carry a `limitation` string whose first token is a shipped AC id.
- Invented anchor fallbacks removed: `probe-utils.emptyResultsFail` and the `runShim` error branch no longer synthesise `anchorAcId: 'unknown'`.
- Skip helpers name exactly one variable in `reason` and distinguish `unset` from `set-but-not-true (observed value ...)`. Anatomy asserts the reason is one of the declared gate variables in the fixture README env-vars section.
- `probe-utils.mjs` accepts `RCF_REPORT_DIR_OVERRIDE` so local runs write to a scratch directory and the tracked `.rcf/reports/` stays byte-identical to `origin/main`.
- Fixture `compose.yaml` declares the `web-token` service-level secret in long form (`- source: web-token`, `target: web-token`, `mode: 0400`) and the compose-stack driver `chmod 400`s the shipped `secrets/web-token` on the throwaway server after rsync but before `docker compose up -d`. Docker Compose (non-Swarm) mounts a compose secret with the source file's host mode, so both the long-form `mode:` declaration and the host-side chmod are required for AC-composeHost-secretShape "mounted at 0o400" to be observed in-container.
- `compose-config-lint` docker-engine wrap row carries `file: 'compose.yaml'` in evidence; the missing-healthcheck failure row carries `missingServices` / `expected: 'healthcheck block'` / `observed: 'absent'` so the anatomy identifier-plus-observation check is satisfied on every branch of the probe.
- Anatomy test regex for the compose secret matches the long-form declaration; anatomy test titles are plain descriptive strings (blueprint slug + AC id + one-line summary), and paired `testPointer` fields in `packages/rcf-lite/rcf/test-suites/ts-150.json` are updated in the same commit.

# platform-docker-compose-host CHANGELOG

## 1.1.2 - 2026-09-10

Register patch.

- Neutral register in README fixture-extension prose (F-2).

## 1.1.1 (register-sweep patch, 2026-09-10)

- Register: neutral wording in shipped prose (no capability change).

## 1.1.0 - 2026-09-09

### Added

- `platform-docker-compose-host-REQ-007` (`must`): the Docker Engine installation follows the elicited apt channel (`stable` or `test`) with the installed repository source as the observable oracle. Closes criterion a on the previously unbacked `docker-engine-channel` elicit (F-1).
- `platform-docker-compose-host-US-38110` traces REQ-007; two ACs (happy-path per-channel installation, refusal on unsupported channel value); each carries `disposition`, and the vendor-fact AC carries `vendorCitation` to the Docker Engine install-on-Ubuntu documentation verified 2026-09-09.

### Changed

- `REQ-001` extended to name the elicited `compose-file-location` alternative (`compose/compose.yaml`) alongside the shipped `repo-root` default; closes criterion a on the previously unbacked alternative branch (F-2).
- `REQ-003` extended to name every elicited healthcheck kind (`http`, `tcp`, `command`); previously only the `http` shape was named.
- `REQ-005` extended to name the `reverse-proxy=none` topology explicitly (the lint now refuses ANY proxy service block or reload verb when `none` is elicited); closes criterion a on the previously unbacked `none` branch (F-3).
- Every existing REQ carries a `deliveredBy` link into a TAC or ADR (six links). Every existing AC carries `disposition`. `rcf define blueprint lint-consistency` reports zero pass-1 and pass-2 findings.

### Added (criterion b completion, second pass 2026-09-09)

- Elicited `healthcheck-timeout-seconds` added to `blueprint.json` (default 30, floor 5, ceiling 300); REQ-003 description extended to name the elicited answer, closing criterion a on the previously-unbacked healthcheck-timeout branch (F-4).
- Every story on this blueprint now reaches the section 7a AC-set-sufficiency floor. 35 new hand-authored ACs cover the documented failure paths named in the guide and TAC records, per-story range 4-6 (from the 1-2 shipped in the first 1.1.0 pass): US-38101 gains five ACs (network drift closing F-9 first, volume drift closing F-9 second, image drift closing F-9 third, .env drift closing F-9 fourth, compose-file-location branch); US-38102 gains four ACs (SSM integration closing F-10 first, file-based fallback closing F-10 second, wrong-mode secret closing F-10 third, orphan top-level secret); US-38103 gains four ACs (wrong-mode probe closing F-10, orphan-secret probe closing F-10, plaintext in .env, clean-pass baseline); US-38104 gains four ACs (healthcheck-timeout elicit AC closing F-4, tcp kind closing F-6, command kind closing F-6, http kind closing F-6); US-38105 gains three ACs (real-account timeout, teardown discipline, dependency ordering); US-38106 gains three ACs (long-running policy classification closing F-7 first, job policy classification closing F-7 second, missing restart field); US-38107 gains three ACs (Traefik topology closing F-5 first, none topology closing F-5 second, read-only bind-mount); US-38108 gains three ACs (Traefik reload verb closing F-5, none topology no-op, reload window too short); US-38109 gains three ACs (journald marker probe closing F-8 first, loki marker probe closing F-8 second, missing logging block); US-38110 gains three ACs (sources file content, keyring presence, idempotent install). Every new AC carries `disposition`, most carry `ownerRef` into the owning TAC, and vendor-fact ACs carry `vendorCitation` (Docker Engine install, compose healthcheck reference, compose secrets reference, compose logging reference, Caddy docs, Traefik docs, journald docs, Loki docs) with today's verifiedOn date.
- `rcf define blueprint lint-consistency` still reports zero pass-1 and pass-2 findings. Blueprint stays at v1.1.0 on the same unreleased minor.

### Fixed (2026-09-09)

- Register sweep: every `blueprint.json` elicit prompt, guide passage, README row, ADR title/context/decision and probe comment on the blueprint has "a platform reverse-proxy decision", "a development host", "an internal ops host", "shipped tunnel" and "hetzner-shipped-spec-2026-09-07.md" neutralised to concept ("a platform decision", "a common Linux-host baseline that already runs Caddy", "a Cloudflare-tunnel sibling"). CHANGELOG entries keep internal provenance.

## 1.0.0 - 2026-09-08

Initial release. Hetzner blueprint per the Hetzner host specification.

- Mints capability `containerHost` and global topic `containerHostContract`.
- Six REQs, nine USs, four TACs, four ADRs; five Node-only probes.
- Extends the shared throwaway-Hetzner-server fixture with a minimal compose stack (`compose.yaml`, `caddy/Caddyfile`, `secrets/web-token` file mount, `src/serve.mjs` healthchecked stub) plus five `run-<probe>.mjs` delegate shims.
- Compose-config-lint carries four mutation switches (`SIMULATE_MISSING_HEALTHCHECK`, `SIMULATE_UNCLASSIFIED_RESTART`, `SIMULATE_UNCLASSIFIED_LOG_DRIVER`, `SIMULATE_EVENT_SECRECY_LEAK`); secrets-as-files-scan carries `SIMULATE_PLAINTEXT_SECRET`; caddyfile-validate carries `SIMULATE_INVALID_CADDYFILE` and runs `caddy validate` via the `caddy:2` container when a local `caddy` binary is not on PATH.
- Reverse-proxy choice elicited via `reverse-proxy` (caddy default per a platform reverse-proxy decision; traefik and none alternatives); Coolify rejected verbatim per a platform reverse-proxy decision; log driver elicited via `log-driver` (journald default; loki opt-in).
- Consumers: a follow-up work item migrates an internal ops host's remote-library compose stack onto this contract after 0.25.0 ships; blueprint acceptance never depends on that migration.
