{
  "usId": "application-admin-console-US-21109",
  "prdId": "PRD-001",
  "reqId": "application-admin-console-REQ-001",
  "version": "1.0.0",
  "status": "approved",
  "title": "Console apply refuses on a bare SPA with the spec 5.5.1 message and honours --allow-no-auth-yet",
  "asA": "operator applying the admin-console on a project with no auth blueprint yet",
  "iWant": "the apply to refuse with exit 3 and a message naming security-auth-* explicitly, or to accept an --allow-no-auth-yet override that records a scaffolding-note on the sidecar",
  "soThat": "a scaffolding pass can land the console files without hiding the fact that the surfaces will refuse at runtime until an auth blueprint is applied",
  "acceptanceCriteria": [
    {
      "id": "AC-21109-1",
      "testable": true,
      "scope": "runtime",
      "description": "rcf define blueprint add ./blueprints/application-admin-console on a project with no applied blueprint declaring principalDirectory (and without --allow-no-auth-yet) exits 3; the stderr message begins with \"application-admin-console requires at least one applied security-auth-*\", lists the currently-applied blueprints on the project, and prints the \"Suggested next steps\" block including the four shelf auth slugs. No contribution files are written; the sidecar is not created.",
      "disposition": "fixed"
    },
    {
      "id": "AC-21109-2",
      "testable": true,
      "scope": "runtime",
      "description": "With --allow-no-auth-yet passed, the apply proceeds and writes rcf/blueprints/application-admin-console.applied.json with allowNoAuthYet:true and a notes string naming the \"no auth yet\" state; a later rcf define validate reads the sidecar and flags the ungated surfaces.",
      "disposition": "fixed"
    },
    {
      "id": "AC-21109-3",
      "description": "When the manifest elicit `blueprint.json.elicits[id=custom-auth-provides-principal-directory]` is answered `true`, the sidecar written by the elicitation phase records `principalDirectory` as a member of `appliedCapabilities`; when the same elicit is answered `false`, the sidecar records no `principalDirectory` entry in `appliedCapabilities`, per REQ-009. Applying agent sets: the boolean answer to `custom-auth-provides-principal-directory`.",
      "given": "a project running its own auth with the elicit `custom-auth-provides-principal-directory` presented at apply",
      "when": "the elicitation phase runs and writes the sidecar",
      "then": "the sidecar the shell reads records `principalDirectory` in `appliedCapabilities` when the answer is `true` and omits it when the answer is `false`",
      "testable": true,
      "scope": "runtime",
      "disposition": "template",
      "ownerRef": {
        "tacId": "TAC-2202-application-admin-console-capability-discovery",
        "field": "responsibilities.runElicitationPhase"
      }
    },
    {
      "id": "AC-21109-4",
      "description": "When the manifest elicit `blueprint.json.elicits[id=custom-auth-provides-role-model]` is answered `true`, the sidecar written by the elicitation phase records `roleModel` as a member of `appliedCapabilities`; when the same elicit is answered `false`, the sidecar records no `roleModel` entry in `appliedCapabilities`, per REQ-009. Applying agent sets: the boolean answer to `custom-auth-provides-role-model`.",
      "given": "a project running its own auth with the elicit `custom-auth-provides-role-model` presented at apply",
      "when": "the elicitation phase runs and writes the sidecar",
      "then": "the sidecar the shell reads records `roleModel` in `appliedCapabilities` when the answer is `true` and omits it when the answer is `false`",
      "testable": true,
      "scope": "runtime",
      "disposition": "template",
      "ownerRef": {
        "tacId": "TAC-2202-application-admin-console-capability-discovery",
        "field": "responsibilities.runElicitationPhase"
      }
    },
    {
      "id": "AC-21109-5",
      "description": "When the manifest elicit `blueprint.json.elicits[id=custom-auth-provides-tenancy]` is answered `true`, the sidecar written by the elicitation phase records `tenancy` as a member of `appliedCapabilities`; when the same elicit is answered `false`, the sidecar records no `tenancy` entry in `appliedCapabilities`, per REQ-009. Applying agent sets: the boolean answer to `custom-auth-provides-tenancy`.",
      "given": "a project running its own auth with the elicit `custom-auth-provides-tenancy` presented at apply",
      "when": "the elicitation phase runs and writes the sidecar",
      "then": "the sidecar the shell reads records `tenancy` in `appliedCapabilities` when the answer is `true` and omits it when the answer is `false`",
      "testable": true,
      "scope": "runtime",
      "disposition": "template",
      "ownerRef": {
        "tacId": "TAC-2202-application-admin-console-capability-discovery",
        "field": "responsibilities.runElicitationPhase"
      }
    },
    {
      "id": "AC-21109-6",
      "description": "When the manifest elicit `blueprint.json.elicits[id=custom-auth-provides-audit-log]` is answered `true`, the sidecar written by the elicitation phase records `auditLog` as a member of `appliedCapabilities`; when the same elicit is answered `false`, the sidecar records no `auditLog` entry in `appliedCapabilities`, per REQ-009. Applying agent sets: the boolean answer to `custom-auth-provides-audit-log`.",
      "given": "a project running its own auth with the elicit `custom-auth-provides-audit-log` presented at apply",
      "when": "the elicitation phase runs and writes the sidecar",
      "then": "the sidecar the shell reads records `auditLog` in `appliedCapabilities` when the answer is `true` and omits it when the answer is `false`",
      "testable": true,
      "scope": "runtime",
      "disposition": "template",
      "ownerRef": {
        "tacId": "TAC-2202-application-admin-console-capability-discovery",
        "field": "responsibilities.runElicitationPhase"
      }
    }
  ],
  "tacIds": [
    "TAC-2202-application-admin-console-capability-discovery"
  ],
  "tags": [
    "blueprint:application-admin-console"
  ],
  "createdAt": "2026-09-05T01:30:00Z",
  "updatedAt": "2026-09-05T01:30:00Z"
}