{
  "usId": "delivery-ci-workflows-US-6108",
  "prdId": "PRD-001",
  "reqId": "delivery-ci-workflows-REQ-008",
  "version": "1.0.0",
  "status": "approved",
  "title": "A missing gate report is never treated as an implicit pass",
  "asA": "reviewer trusting the aggregate report as the sole ledger of what actually ran",
  "iWant": "the aggregate verdict to be `failed` on any missing required gate report",
  "soThat": "a runner crash, an unhandled exception, or a write failure cannot be indistinguishable from success",
  "acceptanceCriteria": [
    {
      "id": "AC-6108-1",
      "description": "The aggregate report records the fixed configured set of required gates as its `gates[]` array in configured order. A required gate whose per-gate report file was not written during the run is recorded in the array with `outcome: missing` and a `reason` field naming the failure mode.",
      "given": "a suite invocation in which the `coverage-strict` gate's Node child process was killed before its report could be written (or the report directory was not writable, or the child exited with an uncaught exception)",
      "when": "the aggregate report is written at the end of the run",
      "then": "the aggregate report's `gates[]` array contains a `coverage-strict` entry with `outcome: missing`, its `reason` field carries a non-empty string, and the aggregate `verdict` is `failed`",
      "testable": true,
      "scope": "runtime",
      "disposition": "template",
      "templateFillIns": "The applying agent sets the elicited report directory; the AC substitutes the project's path for the illustrative default."
    },
    {
      "id": "AC-6108-2",
      "description": "The Node entry point's process exit code is non-zero when any required gate's outcome is `missing`, even if no gate outcome is `failed`.",
      "given": "a suite invocation whose required-gate set has one gate outcome `missing` and every other outcome `passed`",
      "when": "the Node entry point completes",
      "then": "the process exit code is non-zero, and the aggregate report's `verdict` is `failed`",
      "testable": true,
      "scope": "runtime",
      "disposition": "fixed"
    },
    {
      "id": "AC-6108-3",
      "description": "An interrupted aggregate report rewrite (SIGKILL of the aggregate step mid-write) never leaves a stale passing aggregate standing for the current run: after interruption, the aggregate is either the current run's fully-written value OR absent; the pipeline treats an absent aggregate as missing per REQ-008 and refuses a pipeline pass.",
      "given": "a reportDir containing a passing aggregate from a previous run and an aggregate step about to write for the current run",
      "when": "the aggregate writer is SIGKILLed mid-way through writing, then the pipeline evaluates the aggregate",
      "then": "the aggregate is either the new run's fully-written value or absent; the pipeline refuses the pass when absent; the previous run's aggregate never survives",
      "testable": true,
      "scope": "runtime",
      "disposition": "template",
      "templateFillIns": "The applying agent supplies the elicited reportDir path; the AC substitutes it for the illustrative default.",
      "ownerRef": {
        "tacId": "TAC-703-delivery-ci-workflows-aggregate-report",
        "field": "responsibilities[3]"
      }
    }
  ],
  "tacIds": [
    "TAC-701-delivery-ci-workflows-gate-runner",
    "TAC-703-delivery-ci-workflows-aggregate-report"
  ],
  "createdAt": "2026-08-24T00:00:00Z",
  "updatedAt": "2026-09-09T00:00:00Z"
}
