{
  "usId": "deploy-hetzner-server-US-37108",
  "prdId": "PRD-001",
  "reqId": "deploy-hetzner-server-REQ-005",
  "version": "1.0.0",
  "status": "approved",
  "title": "Snapshot-on-demand fires a tagged snapshot and emits hetznerSnapshotTaken",
  "asA": "operator whose project needs an on-demand recovery point at any moment",
  "iWant": "the snapshot-on-demand verb on the provisioner to shell to hcloud image create-image (or the raw-api equivalent) and tag the snapshot with server name and wall-clock time; hetznerSnapshotTaken carries snapshot id and time",
  "soThat": "I have a documented on-demand recovery point at any moment without a manual dashboard visit",
  "acceptanceCriteria": [
    {
      "id": "AC-37108-1",
      "description": "real-account-snapshot-on-demand.mjs (accountBound: true, skipped without CI_HAS_HETZNER_ACCOUNT) fires the snapshot verb on the throwaway server, then asserts hcloud image list --type=snapshot --output json shows a snapshot tagged with the server name and current wall-clock time; hetznerSnapshotTaken event carries snapshot id and time. Teardown via destroy.mjs.",
      "testable": true,
      "scope": "runtime",
      "disposition": "fixed",
      "vendorCitation": {
        "url": "https://docs.hetzner.cloud/#images-create-image-from-a-server",
        "verifiedOn": "2026-09-09"
      }
    },
    {
      "id": "AC-37108-2",
      "description": "Snapshot-on-demand upstream failure. Given the hetzner API returns a non-2xx response on hcloud image create-image (for example 429 rate-limited or 503 upstream), when the snapshot-on-demand verb runs, then the facade emits provisionerFailed with { verb: \"createImage\", apiStatus, retryable } on the event sink and no hetznerSnapshotTaken fires; no orphan snapshot resource remains on the account.",
      "given": "the hetzner API returns 429 or 503 on hcloud image create-image",
      "when": "the snapshot-on-demand verb runs",
      "then": "the facade emits provisionerFailed with { verb, apiStatus, retryable }; no hetznerSnapshotTaken fires; no orphan snapshot remains",
      "testable": true,
      "scope": "runtime",
      "ownerRef": {
        "tacId": "TAC-3801-deploy-hetzner-server-provisioner",
        "field": "interfaces.default"
      },
      "disposition": "fixed",
      "vendorCitation": {
        "url": "https://docs.hetzner.cloud/#errors",
        "verifiedOn": "2026-09-09"
      }
    },
    {
      "id": "AC-37108-3",
      "description": "Snapshot payload shape check. Given a successful snapshot-on-demand, when the hetznerSnapshotTaken event record is inspected, then the record carries exactly { serverName, snapshotId, ts } (ts is wall-clock milliseconds since epoch) and no additional field; a mutation SIMULATE_SNAPSHOT_EVENT_LEAK=true injects a token substring into the record and the sink observer FAILS.",
      "given": "a successful snapshot-on-demand event fired",
      "when": "the record is inspected",
      "then": "the record carries exactly {serverName, snapshotId, ts} and no additional field; the leak mutation FAILS",
      "testable": true,
      "scope": "runtime",
      "ownerRef": {
        "tacId": "TAC-3801-deploy-hetzner-server-provisioner",
        "field": "interfaces.default"
      },
      "disposition": "fixed",
      "vendorCitation": {
        "url": "https://docs.hetzner.cloud/#images-create-image-from-a-server",
        "verifiedOn": "2026-09-09"
      }
    }
  ],
  "tacIds": [
    "TAC-3801-deploy-hetzner-server-provisioner"
  ],
  "createdAt": "2026-09-08T00:00:00.000Z",
  "updatedAt": "2026-09-09T00:00:00Z"
}
