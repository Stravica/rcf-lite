{
  "usId": "persistence-data-d1-US-13101",
  "prdId": "PRD-001",
  "reqId": "persistence-data-d1-REQ-001",
  "version": "1.0.0",
  "status": "approved",
  "title": "One facade module is the only place the D1 binding is read",
  "asA": "developer who needs the domain code to stay swappable and testable across the persistence boundary",
  "iWant": "one facade module to hold the D1 binding and expose named domain verbs, with no consumer reaching into env for the binding directly",
  "soThat": "the boundary between the domain and the D1 vendor surface is the only place that has to move when the vendor changes and the only place tests have to double",
  "acceptanceCriteria": [
    {
      "id": "AC-13101-1",
      "description": "Exactly one source-tree module reads the D1 binding from the Worker `env`. Every other module that touches persistence imports the facade module and calls its named domain verbs.",
      "given": "the running source tree after a Worker request is handled",
      "when": "the import graph and source tree are inspected for the D1 binding reference (`env.<BINDING_NAME>` where BINDING_NAME is the value configured on `d1_databases[].binding` in the project's wrangler config)",
      "then": "the reference appears in exactly one file (the facade module) and no other file in the source tree contains the reference, verified by an import-graph query or a targeted grep against the configured binding name",
      "testable": true,
      "scope": "runtime",
      "disposition": "template"
    },
    {
      "id": "AC-13101-2",
      "description": "The facade exposes named domain verbs for every persistence operation the domain requires; the facade module does not export the raw D1 binding or a general-purpose query method that accepts a raw SQL string from consumer code.",
      "given": "the facade module's public surface",
      "when": "the surface is inspected",
      "then": "every exported symbol is either a named domain verb with typed parameters or an event-sink registration; no exported symbol is the D1 binding itself, and no exported function accepts a raw SQL string as a call argument from consumer code",
      "testable": true,
      "scope": "runtime",
      "disposition": "template"
    },
    {
      "id": "AC-13101-3",
      "description": "Replacing the facade module with an in-memory double that implements the same verb surface allows the domain code to run against it without any reference to the D1 binding.",
      "given": "a test double implementing the same named verbs as the facade with an in-memory backing store",
      "when": "the domain code runs against the double in place of the facade",
      "then": "the domain code executes to completion with no reference to the D1 binding on its call path, and its behaviour is unchanged from a facade-backed run for the operations under test",
      "testable": true,
      "scope": "runtime",
      "disposition": "template"
    },
    {
      "id": "AC-13101-4",
      "description": "Dependency not ready: D1 binding absent from the Worker env. Given a Worker whose wrangler manifest omits the configured d1_databases binding name (or whose deployed environment has not provisioned the binding), when the facade opens on isolate boot and reads env[<bindingName>], then the read returns undefined, the facade refuses with a named error kind d1BindingMissing carrying the expected binding name (never carrying the account id or API token), and no facadeReady fires on the sink.",
      "given": "the Worker env has no property under the configured D1 binding name",
      "when": "the facade opens on isolate boot",
      "then": "the facade refuses with d1BindingMissing naming the expected binding; no credentials leak; no facadeReady fires",
      "testable": true,
      "scope": "runtime",
      "ownerRef": {
        "tacId": "TAC-1401-persistence-data-d1-facade",
        "field": "responsibilities.readBinding"
      },
      "disposition": "template"
    },
    {
      "id": "AC-13101-5",
      "description": "Rate limit from D1: the vendor 429 surfaces as queryFailed with retryAfterMs. Given the D1 vendor returns a 429 rate-limit response on a prepare-and-bind or batch call (simulated in the fixture via SIMULATE_D1_RATE_LIMIT=true), when the facade issues the query, then the facade catches the vendor error, surfaces a queryFailed event on the sink carrying { errorKind: 'rateLimited', retryAfterMs: <parsed from vendor headers or a default>, verb: <domain verb name> } and re-throws a d1RateLimited error the consumer catches; a probe FAILS if the event's errorKind is anything other than rateLimited.",
      "given": "the D1 vendor returns 429 on a facade query (SIMULATE_D1_RATE_LIMIT=true)",
      "when": "the facade issues the query",
      "then": "queryFailed fires with { errorKind: 'rateLimited', retryAfterMs, verb }; the facade re-throws d1RateLimited; the probe FAILS on any other errorKind",
      "testable": true,
      "scope": "runtime",
      "ownerRef": {
        "tacId": "TAC-1401-persistence-data-d1-facade",
        "field": "responsibilities.queryFailedRateLimit"
      },
      "disposition": "template"
    }
  ],
  "tacIds": [
    "TAC-1401-persistence-data-d1-facade"
  ],
  "createdAt": "2026-08-30T00:00:00Z",
  "updatedAt": "2026-09-09T00:00:00Z"
}
