{
  "name": "rcf-lite-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "pnpm workspace for the RCF Lite tooling monorepo. Houses @stravica-ai/rcf-build-lite (packages/build) and future workspace packages.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "test": "pnpm -r test"
  }
}
