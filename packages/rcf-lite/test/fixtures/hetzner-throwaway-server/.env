# Non-secret configuration for the T-2 compose fixture (round-7 platform-docker-compose-host).
# Secrets are NEVER declared here; they live under secrets/ and are mounted via compose secrets: file mounts.
WEB_APP_NAME=rcf-lite-throwaway-web
WEB_LISTEN_PORT=8080
WEB_HEALTH_PATH=/live
